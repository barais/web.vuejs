{
  "name": "vuejs",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export",
    "deploygithub": "slidev build --base /web.vuejs/ --download && rm -rf docs && mv dist docs && git add docs/** && git commit . -m 'update' && git push"

  },
  "dependencies": {
    "@slidev/cli": "^0.49.26",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "vue": "^3.4.38"
  }
}